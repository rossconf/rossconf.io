sudo: false
language: ruby
cache:
  bundler: true
  directories:
  - build
rvm:
  - 2.2.2
branches:
  only:
  - master
script:
  - bundle exec rake travis:script
after_success:
  - bundle exec rake travis:after_success
